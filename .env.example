# LLM proxy target
ANTHROPIC_BASE_URL=http://localhost:8001/v1
ANTHROPIC_API_KEY=dummy

# Model served by vLLM / SGLang
MODEL_NAME=meta-llama/Llama-3.1-8B-Instruct

# GPU configuration: MI355X | 4xH100
GPU_TYPE=4xH100

# Optional: EAGLE speculative decoding draft model (SGLang only)
EAGLE_MODEL=
